resources:
  dashboards:
    orbital_sense_dashboard:
      display_name: "OrbitalSense Telemetry Analytics"
      file_path: ../src/dashboards/orbital_sense_analytics.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.schema}
      permissions:
        - level: CAN_READ
          group_name: "users"
